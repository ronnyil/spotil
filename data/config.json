{
  "_comment": "Set adsbProxy to the FULL endpoint URL of your deployed proxy, e.g. https://<project>.vercel.app/api/adsb (see README). The community ADS-B networks send no CORS header, so without a proxy the site falls back to the time-of-day prediction. Leave it empty to run without one.",
  "adsbProxy": "http://127.0.0.1:8795/api/adsb"
}